PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://finsight:finsight@localhost:5432/finsight
REDIS_URL=redis://localhost:6379
FRONTEND_URL=http://localhost:3000
# Optional: comma-separated extra origins allowed by CORS (e.g. preview URLs).
CORS_EXTRA_ORIGINS=
# Optional: regex matched against request origin for dynamic preview deployments.
# Defaults to the Vercel project pattern if unset.
CORS_ORIGIN_REGEX=^https://fin-sight-frontend-[a-z0-9-]+\.vercel\.app$
CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
AI_SERVICE_URL=http://127.0.0.1:8000
